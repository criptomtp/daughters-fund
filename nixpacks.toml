[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

# NOTE: the runtime start command is defined in railway.json (deploy.startCommand),
# which overrides any Nixpacks [start] block, so it is intentionally omitted here.
